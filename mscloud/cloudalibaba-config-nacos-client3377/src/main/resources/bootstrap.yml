server:
  port: 3377

spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml  #指定文件的后缀
        group: DEV_GROUP  # 读取的分组名称
        namespace: 8292cb5a-51a3-49df-85a5-5e8c698de754
