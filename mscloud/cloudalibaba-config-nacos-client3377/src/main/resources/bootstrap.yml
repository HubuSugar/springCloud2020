server:
  port: 3377

spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml  #指定文件的后缀
        group: DEV_GROUP  # 读取的分组名称
        namespace: 1482f062-049d-49d2-8281-0d9860262a7e
