server:
  port: 9003

spring:
  application:
    name: payment-provider-service

  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

management:
  endpoints:
    web:
      exposure:
        include: '*'
